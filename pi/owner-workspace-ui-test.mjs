import assert from 'node:assert/strict';
import fs from 'node:fs';

const html=fs.readFileSync(new URL('../index.html',import.meta.url),'utf8');
const app=fs.readFileSync(new URL('../app.js',import.meta.url),'utf8');
assert.match(html,/id="ownerAccess"/);
assert.match(html,/class="sync-button owner-access-button"/);
assert.match(html,/>Owner Control</);
assert.match(html,/>Secure sign in</);
assert.match(html,/Owner\? Use <strong>Owner Control<\/strong>/);
assert.match(html,/id="ownerDialog"/);
assert.match(html,/type="password"/);
assert.match(app,/\/api\/owner\/login/);
assert.match(app,/\/api\/owner\/workspace/);
assert.match(app,/sessionStorage\.setItem\(OWNER_SESSION_KEY/);
assert.doesNotMatch(app,/localStorage\.setItem\(OWNER_SESSION_KEY/);
assert.match(app,/workspace_revision_conflict/);
assert.match(app,/Authenticated owner workspace is active/);
assert.match(html,/id="ownerCommandCenter"/);
assert.match(html,/data-owner-view-target="overview"/);
assert.match(html,/data-owner-view-target="krishna"/);
assert.match(html,/data-owner-view-target="execution"/);
assert.match(html,/data-owner-view-target="intelligence"/);
assert.match(html,/id="ownerKrishnaActivity"/);
assert.match(app,/function setOwnerView/);
assert.match(app,/Shows operational state and evidence, not private reasoning/);
assert.match(html,/class="owner-command-center hidden"/);
assert.match(app,/\/api\/owner\/dashboard/);
assert.match(app,/refreshOwnerCommandCenter/);
assert.match(app,/productionActivationVerified/);
assert.match(app,/customerChargingVerified/);
assert.match(html,/id="ownerProgressChart"/);
assert.match(html,/id="ownerSignOut"/);
assert.match(html,/id="ownerActionList"/);
assert.match(html,/id="ownerLastUpdated"/);
assert.match(app,/renderOwnerProgressChart/);
assert.match(app,/ownerCommandCenter\?\.scrollIntoView/);
assert.match(app,/document\.body\.classList\.add\('owner-mode'\)/);
assert.match(app,/ownerSignOut\?\.addEventListener/);
assert.match(html,/id="ownerChartTooltip"/);
assert.match(html,/id="ownerGrowthPercent"/);
assert.match(html,/id="ownerDailyGrowth"/);
assert.match(html,/id="ownerSecurityStatus"/);
assert.match(app,/growthIndex/);
assert.match(app,/onpointerdown/);
assert.match(app,/ownerChartTooltip/);
assert.match(app,/ownerSecurityStatus/);
assert.match(html,/id="ownerChartTooltip"/);
assert.match(html,/id="ownerGrowthPercent"/);
assert.match(html,/id="ownerDailyGrowth"/);
assert.match(html,/id="ownerSecurityStatus"/);
assert.match(app,/growthIndex/);
assert.match(app,/onpointerdown/);
assert.match(app,/ownerChartTooltip/);
assert.match(app,/ownerSecurityStatus/);
const workspaceStart=html.indexOf('<section class="workspace"');
const ownerCenter=html.indexOf('id="ownerCommandCenter"');
const workspaceEnd=html.indexOf('</section>',ownerCenter);
assert.ok(workspaceStart >= 0 && ownerCenter > workspaceStart && workspaceEnd > ownerCenter,'owner_dashboard_must_be_inside_scroll_workspace');
console.log('PI owner workspace UI contract tests passed.');
